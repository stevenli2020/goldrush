# L8-001 Phase 3 handoff

The WGC ETF workbook's monthly aggregate `Tonnes` column is passed through as
deterministic net-flow values in metric tonnes. Negative values are valid net
outflows; the series is distinct from ETF holdings. No GLD/IAU market proxy or
synthetic flow is introduced.

# L9-001 Changelog

## 2026-08-24

- Added fixture-testable WGC China premium/discount workbook parser, schema, config, fallback, revision handling, and documentation.
- Current WGC workbook download returned HTTP 403 in the available session; no live L9-001 evidence is claimed.
- Grace rework: prior-data fallback now returns `STALE` end-to-end and clears stale `BLOCKED` artifacts; non-finite values are rejected. Unverified smoothing and China-specific January 2025 revision claims were removed. Added reproducible `create_manifest.py`; zero-source metadata sample is explicitly fixture-only. Live workbook access remains blocked by HTTP 403.
- Shared WGC integration: added the `gold_premiums` download target and manifest pass-through to `wgc_extract.py`; the parser now selects the verified `Chinese premiums-discounts` sheet and checks the title, US$/oz unit, and five-day moving-average definition.
- Live run 2026-08-24: HTTP 200, XLSX, 319,907 bytes, source metadata ``; 6,088 rows from 2003-04-25 through 2026-08-21; latest value `-2.003030999999828`, `PASS`/`AVAILABLE`. All rows passed typed schema validation. Fallback, `BLOCKED`, recovery, and extractor dispatch were replayed successfully.
- Grace rework: shared dispatch now passes the processed output as `--prior`, unchanged `gold_premiums` runs refresh availability status, and manual manifests leave HTTP status/content type unknown unless observed. Completed-variable outputs regenerated by forced regression were restored.
- Verification update: L9/shared-WGC tests now pass 11 total, including shared dispatch failure-with-prior and unchanged-run availability refresh integration coverage.
- Final approval: Grace approved L9-001 as Complete. Routine shared-WGC collection and documented manual fallback are now the accepted operational paths.

# L9-001 samples

`processed_sample.csv` is a schema-shaped fixture illustrating positive and
negative Chinese observations. Its zero source metadata and size are deliberately not
live provenance. The real-layout workbook fixture is generated in
`tests/test_l9_parser.py`; production provenance comes only from the shared WGC
manifest or the canonical manual `create_manifest.py` fallback.
